# Copy to .env. The "brain" connects to your LLM pool on the server over SSH.
# No secrets here — SSH uses your existing key (~/.ssh/config alias, default: vps).
BRAIN_ENABLED=1
BRAIN_SSH_HOST=vps
BRAIN_REMOTE_CMD=python3 /root/agents/common/brat_brain_route.py
BRAIN_SSH_TIMEOUT=25
